This experiment was done by Sam Stockmann (sjsmdd) and Lavender Shannon (jdsfz4).

This project uses more files than required. Using more files allows for the separation of more layers of logic for better abstraction.

sd depends on sd_commands, which depends on sd_spi, which depends on spi.
